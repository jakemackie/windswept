{
  "name": "windswept",
  "version": "1.0.0",
  "description": "A production-grade Typescript Discord bot built to run on the newest tooling for a lovely, lightweight dev experience.",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "dev": "doppler run -- docker compose -f docker-compose.dev.yml up --build",
    "stop": "docker compose -f docker-compose.dev.yml down",
    "clean": "docker compose down -v",
    "deploy": "doppler run -- node ./scripts/deploy-commands.js",
    "migrate:dev": "doppler run -- npx prisma migrate dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/node": "^24.0.12",
    "prisma": "^6.11.1",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@prisma/client": "^6.11.1",
    "discord.js": "^14.21.0",
    "dotenv": "^17.0.1"
  },
  "prisma": {
    "schema": "./prisma"
  }
}
