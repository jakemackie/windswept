{
  "name": "windswept",
  "version": "1.1.0",
  "description": "A production-grade Typescript Discord bot built to run on the newest tooling for a lovely, lightweight dev experience.",
  "main": "index.js",
  "scripts": {
    "docker": "docker compose -p windswept --file docker/docker-compose.yml",

    "db:start": "npm run docker -- up -d",
    "db:stop": "npm run docker -- down",
    "db:wipe": "npm run docker -- down -v",
    "db:migrate": "doppler run -- npx prisma migrate deploy",
    "db:generate": "doppler run -- npx prisma generate",
    "db:studio": "doppler run -- npx prisma studio",
    "db:wait-for-db": "node ./.config/scripts/wait-for-db.js",
    "db:deploy": "doppler run -- node ./.config/scripts/deploy-commands.js",

    "bot:start": "doppler run -- robo dev",

    "dev": "npm run db:generate && npm run bot:start",
    "dev:lint": "biome lint . --max-diagnostics=1000 --config-path .config/biome.json",
    "dev:fix": "biome lint --write . --max-diagnostics=1000 --config-path .config/biome.json"
  },
  "keywords": [],
  "author": "jakemackie",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@biomejs/biome": "2.1.4",
    "@types/node": "^24.2.0",
    "pg": "^8.16.3",
    "prisma": "^6.13.0",
    "rolldown": "^1.0.0-beta.31",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@prisma/client": "^6.13.0",
    "@swc/core": "^1.13.3",
    "discord.js": "^14.21.0",
    "dotenv": "^17.2.1",
    "fast-average-color-node": "^3.1.0",
    "robo.js": "^0.10.30"
  }
}
